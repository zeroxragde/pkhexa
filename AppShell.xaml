<Shell
    x:Class="PkHexA.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PkHexA"
    
    x:Name="winAppShell">
    <Shell.TitleView>
        <Grid VerticalOptions="Center" HorizontalOptions="FillAndExpand">

            <Label x:Name="lblTituloApp" 
                   Text="PkHexA" 
                   TextColor="White"
                   FontSize="20"
                   FontAttributes="Bold"
                   VerticalOptions="Center"
                   HorizontalOptions="Start" 
                   Margin="10,0"/>

        </Grid>
    </Shell.TitleView>

    <ShellContent
        Title="Home"
        x:Name="shellHome"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>